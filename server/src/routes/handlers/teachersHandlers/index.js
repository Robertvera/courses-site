const createTeacher = require('./createTeacher')
const editTeacher = require('./editTeacher')
const removeTeacher = require('./removeTeacher')
const retrieveTeacher = require('./retrieveTeacher')
const listTeachers = require('./listTeachers')

module.exports = {
    createTeacher,
    editTeacher,
    removeTeacher,
    retrieveTeacher,
    listTeachers
}
